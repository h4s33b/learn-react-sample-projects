import React from 'react';
import ReactDOM from 'react-dom';
import TimerWrapper from './wrapper/wrapper';
import './index.css';

ReactDOM.render(
  <TimerWrapper />,
  document.getElementById('root')
);
